echo "Checking import case (catches Linux-only build errors that Windows hides)..."
npm run check:imports || {
  echo ""
  echo "Push aborted: fix the case mismatches above."
  echo "Windows treats foo.png and Foo.png as the same file. Linux CI does not."
  exit 1
}

echo ""
echo "Running production build..."
npm run build || {
  echo ""
  echo "Push aborted: production build failed. See errors above."
  exit 1
}

echo ""
echo "All checks passed. Pushing..."
